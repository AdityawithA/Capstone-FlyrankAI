# Project Rules

- Use Next.js App Router conventions.
- Prefer Server Components unless interactivity requires a Client Component.
- Keep routes small and focused.
- Use Tailwind CSS for UI styling.
- Keep secrets out of source control.
- Run `npm run build` before submission.
- Test both `/` and `/health` locally and on the deployed preview.
- Do not introduce dependencies without a clear reason.
