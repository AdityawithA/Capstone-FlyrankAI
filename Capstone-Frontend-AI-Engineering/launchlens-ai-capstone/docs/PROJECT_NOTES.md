# Project Notes

This capstone is intentionally scoped as one complete workflow instead of a large dashboard.

**Product:** LaunchLens AI  
**Primary value:** convert product ambiguity into testable decisions  
**AI:** Gemini via server-side Next.js route  
**Deployment target:** Vercel  
**Test runner:** Vitest  
**Accessibility:** semantic HTML + keyboard/focus + live regions + reduced motion

Before submission, replace the placeholders in `README.md` and `docs/SUBMISSION_ENTRY.md` with the real GitHub repository and deployed URL, then paste the measured Lighthouse and axe/WAVE results into the audit document.
